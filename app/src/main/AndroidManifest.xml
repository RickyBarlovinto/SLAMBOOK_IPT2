<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MySlamBook.NoActionBar"
        tools:targetApi="31">

        <!-- Activities -->
        <activity
            android:name="barlovinto.ricky.myslambook.FormActivity"
            android:exported="false" />
        <activity
            android:name="barlovinto.ricky.myslambook.Form3Activity"
            android:exported="false" />
        <activity
            android:name="barlovinto.ricky.myslambook.Form2Activity"
            android:exported="false" />
        <activity
            android:name="barlovinto.ricky.myslambook.Form1Activity"
            android:exported="false" />
        <activity
            android:name="barlovinto.ricky.myslambook.ListEntriesActivity"
            android:exported="false" />
        <activity
            android:name="barlovinto.ricky.myslambook.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="barlovinto.ricky.myslambook.MainActivity"
            android:exported="false" />

        <!-- FileProvider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>